import React from 'react'
import Modal from '../../Modal/Modal'

export default function EditSettinsModal({settings,isOpen,setIsOpen,title}) {
  return (
    <Modal isOpen={isOpen} setIsOpen={setIsOpen} title={title}>
        
    </Modal>
    )
}
